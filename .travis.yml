language: node_js

node_js:
  # latest stable node version
  - "stable" 

# whitelist
branches:
 only:
   - master
  
before_script:
  - cd server
  - npm install

script:
  - node server.js &
  - npm test